{% extends('AppDashboardBundle::empty.html.twig') %}

{% block meta %}<meta HTTP-EQUIV="refresh" CONTENT="5">{% endblock %}

{% block title %}{{ title }}{% endblock %}

{% block body %}

    <div class="center-block well">
    {% if message %}
        <p class="text-center text-muted">{{ message|raw }}</p>
    {% endif %}
    </div>

    <ul>
        <li><a href="{{ path('admin_security') }}">Go back</a></li>
    </ul>
{% endblock %}

